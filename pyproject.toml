[tool.poetry]
name = "apiregisena"
version = "0.1.0"
description = ""
authors = ["Tu Nombre <espinosa081205@gmail.com>"]

[tool.poetry.packages]
include = [{ include = "app.py" }] 

[tool.poetry.dependencies]
python = "^3.10"  # Usa 3.8, 3.9 o 3.10 (3.13 puede dar problemas)
flask = "^2.0.0"  # Ejemplo: añade tus dependencias aquí

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"